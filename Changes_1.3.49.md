# PlanSlayer V1.3.49

## UX
1. **Show all / type chips** — nudged ~5% right (`padding-left: 5%`) so they aren’t clipped under left chrome.
2. **My Color** — regular muted palette (no neon presets). Selecting a color turns **Close → Save**; removed the separate Save nickname button. Footer is **Sign out | Close/Save** side by side.
3. **Header** — **Other ways to slay** sits to the right of the version badge. **Join** sits next to **Report an issue** under the username (not between user and Other ways).

## Shell
- `plan-slayer-shell-v82`
